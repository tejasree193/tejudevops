this is my first project
git init
git status
git add .
git remote add origin 'url fo repository'
git config --global user.name "tejasree193"
git config --global user.mail "maidalatejasree@gmail.com"
git commit -m "saved"
git push -u origin master
git pull
git clone "url"
git branch
git checkout -b "MITS"
git commit -m "new branch created"
git push --set-upstream origin MITS
git merge MITS
git log
git log --oneline


name: Python Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.10"

      - name: Run test
        run: python main.py

git init
git remote -v
git config --global user.name
git config --global user.mail
git remote add origin
mkdir .github
cd .github
mkdir workflows
cd workflows
gii.yml
cd ..
git commit -m "hi"
git branch -m main
